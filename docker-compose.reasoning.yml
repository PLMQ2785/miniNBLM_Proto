# Reasoning benchmark adds the local sample corpus as a read-only mount.
services:
  benchmark:
    environment:
      VISION_CAPTION_MODE: ${VISION_CAPTION_MODE:-disabled}
    volumes:
      - ./sample:/app/sample:ro
