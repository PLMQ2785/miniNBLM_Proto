[project]
name = "mininblm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12, <3.13"
dependencies = [
]

[dependency-groups]
common = [
    "fastapi==0.115.14",
    "pydantic-settings==2.10.1",
    "uvicorn[standard]==0.35.0",
]
api = [
    { include-group = "common" },
    "alembic==1.16.2",
    "httpx==0.28.1",
    "openai==1.93.0",
    "pgvector==0.4.1",
    "prometheus-client==0.26.0",
    "psycopg[binary]==3.2.9",
    "pwdlib[argon2]>=0.3.0",
    "pymupdf==1.26.3",
    "python-multipart==0.0.20",
    "sqlalchemy==2.0.41",
]
embedding = [
    { include-group = "common" },
    "sentence-transformers==4.1.0",
    "torch==2.7.1",
]
dev = [
    { include-group = "api" },
    "pytest==8.4.1",
]

[tool.uv]
default-groups = ["dev"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
markers = [
    "integration: requires the isolated PostgreSQL test database",
    "e2e: requires real embedding and vLLM services",
]
